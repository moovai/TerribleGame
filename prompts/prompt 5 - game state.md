The following code suffers from poor state management — many game state variables are exposed, loosely grouped, or globally accessible. Your task is to **encapsulate the game state properly**, following clean object-oriented design principles.

---
[insert code here]

---
🎯 **Objective:**

• Encapsulate game state variables (e.g., score, health, level, inventory, etc.) within appropriate classes or data structures.
• Ensure state is **not globally exposed** or **randomly modified** from unrelated parts of the code.
• Add clear **getters/setters or method-based access**, respecting encapsulation boundaries.
• Group related state variables logically (e.g., player state, world state, session state).
• Do **not** change game logic or behaviors — just improve state encapsulation.
• Return the full code in a **single file**, even if it becomes long.
• If the output is too large, continue until the full transformation is complete.