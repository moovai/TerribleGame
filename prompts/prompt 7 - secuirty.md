Analyze and refactor the following code to address two specific security issues:

1. **Prevent SQL injection vulnerabilities**
2. **Avoid storing or handling passwords in plaintext**

---
[insert code here]

---

🔒 **Instructions:**
• Use **parameterized queries** or ORM-safe patterns to eliminate SQL injection risks.
• Ensure passwords are never stored, logged, or transmitted in plaintext.
• Use a secure password hashing function (e.g., bcrypt, argon2, or another appropriate algorithm for the language).
• Do **not** modify other aspects of the code. Focus **only** on fixing these two security concerns.
• Output the complete code in a **single file**.
• If the output is too long, **continue automatically until the full code is included**.